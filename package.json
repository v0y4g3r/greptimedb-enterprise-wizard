{
  "name": "greptimedb-enterprise-wizard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "vue-tsc -b && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "smol-toml": "^1.6.1",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.36.0",
    "@types/js-yaml": "^4.0.9",
    "@vitejs/plugin-vue": "^5.2.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "~5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.9",
    "vue-tsc": "^2.2.0",
    "wrangler": "^4.88.0"
  }
}
